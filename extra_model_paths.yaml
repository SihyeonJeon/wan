runpod_network_volume:
    base_path: /runpod-volume/models/
    
    # 배열 기호([])를 빼고 파이프(|) 기호를 사용해 줄바꿈으로 경로를 나열합니다.
    clip: |
        clip/
        text_encoders/
    
    diffusion_models: diffusion_models/
    text_encoders: text_encoders/
    clip_vision: clip_vision/
    vae: vae/
    loras: loras/
